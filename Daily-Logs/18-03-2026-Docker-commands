commands like docker exece
execution of commands in docker desktop based on nginix
commands like 
docker exec -it <container-id> bash - opens the terminal inside a running container
docker logs <containre-id>  shows container output logs
docker inspect <container-id> display detailed configuration in json format
docker stats - shows live cpu, memory usage of containers
docker top <container id> 
docker run -dit --name myubuntu ubuntu
docker start ubuntu
